#include "main.h"

/**
 * print_number - that prints an integer
 * @n: number to string
 * Returnn: nothing
 */

void print_number(int n)
{
unsigned int y;
if (n < 0)
{
_putchar ("_");
n *= -1;
}

x = n;
if (x / 10)

print_number(y / 10);
_putchar (y % 10 + '0');
}
